import 'package:flutter/material.dart';

class Settinge extends StatelessWidget {
  const Settinge({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Settinge"));
  }
}
